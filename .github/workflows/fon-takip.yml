name: GTZ Fon Takip

on:
  schedule:
    # Her 5 dakikada bir çalışır
    - cron: '*/5 * * * *'
  workflow_dispatch: # Manuel çalıştırma için

jobs:
  check-fund:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout kod
      uses: actions/checkout@v3
    
    - name: Python kurulumu
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Bağımlılıkları yükle
      run: |
        pip install requests urllib3
    
    - name: GTZ fon kontrolü yap
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
      run: |
        python gtz_github_fonanaliz.py
    
    - name: Son değeri kaydet (Git commit)
      run: |
        git config user.name "GitHub Actions Bot"
        git config user.email "actions@github.com"
        git add gtz_son_deger.json
        git diff --quiet && git diff --staged --quiet || git commit -m "🔄 GTZ fiyat güncellendi - $(date '+%Y-%m-%d %H:%M:%S')"
        git push || true
